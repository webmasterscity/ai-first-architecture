// scripts/setup.js
const chalk = require('chalk');

console.log(chalk.green('🚀 AI-First Architecture setup completed!'));
console.log(chalk.blue('Run npm install to install dependencies'));
console.log(chalk.blue('Run npx playwright install to setup visual testing'));
